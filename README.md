
  # High-Fidelity Railway Dashboard

  This is a code bundle for High-Fidelity Railway Dashboard. The original project is available at https://www.figma.com/design/pUZ5gr7yCuBVIaYGraiQ5Y/High-Fidelity-Railway-Dashboard.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  